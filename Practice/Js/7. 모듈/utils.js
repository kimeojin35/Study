// 이름 내보내기
export function add(a, b) {
  return a + b;
}

// Myltiply 클래스를 이름 내보내기
export class Multiply {
  multiply(a, b) {
    return a * b;
  }
}

// 기본 내보내기
export default function subtract(a, b) {
  return a - b;
}
